A basic logistic regression model that predicts the document type of a given document.
Training data are bag of words. Every column is formated as: document_id word_id count.
The labels are the document type (0 or 1) for each document_id.
