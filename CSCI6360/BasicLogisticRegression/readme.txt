The training data is a bag of words data set. 
The columns of train.data and test.data are in the form: document word_id count
The columns of train.label and test.label are two document types.
The logistic regression is used to label every document at type 0 or type 1.
